<%- include('partials/header'); -%> 

<h1>Home</h1>
<p><%= homePageInfo %></p>

<div class="" id="blogPosts">
    <% posts.forEach(post => { %>
         <h2><%= post.title %> </h2>
         <p><%= post.content.substring(0,150) + " ..." %><a href="<%= 'posts/'+post.url %>">Read More</a> </p>
    <% }) %>
</div>

<%- include('partials/footer'); -%> 